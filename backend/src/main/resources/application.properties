spring.application.name=github-manager
# Railway injects PORT at runtime; locally defaults to 8080
server.port=${PORT:8080}

# GitHub token from environment variable (override via X-GitHub-Token request header)
github.token=${GITHUB_TOKEN:}

logging.level.com.github.manager=INFO
logging.level.org.springframework.web=WARN